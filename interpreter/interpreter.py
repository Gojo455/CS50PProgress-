expr = input("Expression: ")
for char in expr:
    if char in "+-*/":
        operator = char
        x = int(expr[:char])
        y = int(expr[char+1:])
        x, y = x.replace(" ",""), y.replace(" ","")

if operator = 
        

